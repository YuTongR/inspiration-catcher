# C 的 Vibe Coding 提示词

目录约定：
- 项目目录：`D:\48\writing\inspiration-catcher`
- 你只负责：`app.py`
- 不要改 A 的 `ingestion.py`
- 不要改 B 的 `agent.py` / `collect.py`
- 当前目标：`streamlit run app.py` 能打开网页，并且两个页面能切换

## D1 任务（今天下午）
目标：先把 C 端双页面前端跑通，页面独立可演示，先用假数据，不依赖 A/B。

---

## 提示词 1：主页面框架
复制下面这段，贴给 TRAE：

```text
帮我写一个 Streamlit 应用 app.py，要求如下：
- 项目路径是 inspiration-catcher/app.py
- 左侧侧边栏有两个选项：“存入素材”和“素材库”
- 存入页：
  - 一个大标题“灵感捕手”
  - 一个 text_area 输入框，高度 300
  - 一个“存入素材库”的按钮
  - 点击按钮后显示成功提示
- 素材库页：
  - 标题“我的素材库”
  - 用卡片样式展示假数据
  - 写死 3 条示例素材，每条包含：标题、标签、摘要、日期
- 用 st.columns 做卡片布局
- 代码要能直接运行
- 先不要依赖 ingestion.py、agent.py、collect.py
- 只输出代码，不要解释
```

---

## 提示词 2：美化卡片样式
复制下面这段，贴给 TRAE：

```text
帮我给上面的 Streamlit 素材卡片加 CSS 样式，要求如下：
- 每张卡片有浅灰背景、圆角边框、内边距
- 标签用彩色小徽章显示，不同标签不同颜色
- 摘要用斜体灰色文字
- 日期用更小的灰色文字显示在底部
- 不要改已有页面逻辑
- 只输出新增或修改后的 app.py 完整代码，不要解释
```

---

## 提示词 3：为后续联调预留说明
复制下面这段，贴给 TRAE：

```text
请在 app.py 里保留清晰注释，说明后续这里要接 B 的 collect.py / materials.db，当前阶段先继续使用本地假数据和 session_state，不要提前写复杂数据库逻辑。
只输出代码，不要解释。
```

---

## 今天的验证标准
在 `D:\48\writing\inspiration-catcher` 目录运行：

```bash
streamlit run app.py
```

满足以下条件就算完成：
- 浏览器能正常打开
- 左侧能切换“存入素材”和“素材库”
- “素材库”里至少有 3 张卡片
- 卡片样式明显区分标题、标签、摘要、日期
- 点击“存入素材库”按钮后会出现成功提示

---

## 你今天作为 C 的交付物
- `app.py`
- 可运行的双页面前端
- 一份可继续给 TRAE 的提示词
